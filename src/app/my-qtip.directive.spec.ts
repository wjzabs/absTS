import { MyQtipDirective } from './my-qtip.directive';

describe('MyQtipDirective', () => {
  it('should create an instance', () => {
    const directive = new MyQtipDirective();
    expect(directive).toBeTruthy();
  });
});
